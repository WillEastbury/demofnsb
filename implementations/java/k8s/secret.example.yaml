apiVersion: v1
kind: Secret
metadata:
  name: attribute-status
  namespace: attribute-status
type: Opaque
stringData:
  AttributeStatusMongoConnectionString: REPLACE_WITH_KEY_VAULT_OR_SECRET_VALUE
  # Optional fallbacks when workload identity is unavailable:
  # ServiceBusConnection: REPLACE_WITH_SERVICE_BUS_CONNECTION_STRING
  # ServiceBusConnectionString: REPLACE_WITH_SERVICE_BUS_CONNECTION_STRING
  # AttributeStatusTableConnectionString: REPLACE_WITH_STORAGE_CONNECTION_STRING
  # AzureWebJobsStorage: REPLACE_WITH_STORAGE_CONNECTION_STRING
