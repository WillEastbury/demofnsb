apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-writer-function
  namespace: message-persistence-sample
spec:
  replicas: 1
  selector:
    matchLabels:
      app: message-writer-function
  template:
    metadata:
      labels:
        app: message-writer-function
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: message-writer
      terminationGracePeriodSeconds: 30
      containers:
        - name: function
          image: ghcr.io/willeastbury/demofnsb-function:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: message-writer
            - secretRef:
                name: message-writer
                optional: true
          ports:
            - name: functions
              containerPort: 80
          readinessProbe:
            tcpSocket:
              port: functions
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: functions
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1Gi
